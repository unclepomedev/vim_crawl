services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - cargo-cache:/usr/local/cargo/registry
    environment:
      - CARGO_TERM_COLOR=always

volumes:
  cargo-cache: