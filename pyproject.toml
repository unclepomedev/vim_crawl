[project]
name = "vim-crawl"
version = "0.0.1"
description = "Python scripts serve as helpers for tasks such as environment setup."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []

[dependency-groups]
dev = [
    "types-houdini>=21.0.512.3",
]
